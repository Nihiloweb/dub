# Local-only base (tag on the Coolify host before deploy):
#   docker tag quay.io/minio/minio:latest dub-minio-base:local
# Avoids quay.io/Docker Hub 401 during build metadata fetch.
FROM dub-minio-base:local
