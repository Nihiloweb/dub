# Merged automatically by `docker compose up` locally (Coolify ignores it).
# Publishes host ports for local development.
services:
  dub:
    ports:
      - "${DUB_PORT:-3000}:3000"
  minio:
    ports:
      - "${MINIO_API_PORT:-9000}:9000"
      - "${MINIO_CONSOLE_PORT:-9001}:9001"
