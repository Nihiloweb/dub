# Merged automatically by `docker compose up` locally.
# Coolify typically passes -f and does not load this file — set build in Coolify UI or bake an image.
services:
  dub:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        DATABASE_URL: mysql://root:${DB_ROOT_PASSWORD:-build}@db:3306/dub
        NEXT_PUBLIC_APP_DOMAIN: ${NEXT_PUBLIC_APP_DOMAIN}
        NEXT_PUBLIC_APP_SHORT_DOMAIN: ${NEXT_PUBLIC_APP_SHORT_DOMAIN}
    image: dub:local
